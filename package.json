{
  "name": "lumen-portal",
  "version": "2.1.0",
  "description": "פורטל לימוד React/JS/TS בעברית — adaptive learning portal",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "validate": "node scripts/validate_bank.js",
    "validate:strict": "node scripts/validate_bank.js --strict",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "happy-dom": "^15.0.0"
  }
}
