# SVCollege Term Clarity

Scope: Finish Line 1 language and term clarity for prerequisite panels and explanation popups.

Checked files:
- `data/svcollege_prerequisites_*.js`
- `data/svcollege_questions_*.js`
- `data/glossary.js`
- `data/concise_definitions.js`

Required terms are covered in `data/glossary.js` with `he`, `short`, `long`, and `category`.

`data/concise_definitions.js` contains one-line `what` definitions for the required terms that need concise repair text.

unknown/unavailable:
- Direct string scan did not find `bit` in the current `data/svcollege_prerequisites_*.js` or `data/svcollege_questions_*.js` files.
- Direct string scan did not find `byte` in the current `data/svcollege_prerequisites_*.js` or `data/svcollege_questions_*.js` files.

Both terms remain defined because they were explicitly listed as required for Finish Line 1.
