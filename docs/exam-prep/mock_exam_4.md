# מבחן מדומה #4 — מבחן קשה במיוחד

**כמות שאלות:** 40 MC + 20 Fill = 60 סה"כ

**זמן מומלץ:** 90 דקות (1.5 דקות לשאלה)

**הוראות:**
1. ענה על כל השאלות. אל תשאיר ריקות.
2. בשאלות MC — סמן רק תשובה אחת.
3. בשאלות Fill — מלא את החסר.
4. שמור על הזמן.

---

## חלק א' — שאלות אמריקאיות (MC)

### 1. מה נכון לתאר את AI לפי השיעור?

- [ ] A. מודל שמחזיר טקסט/קוד בהסתמך על סטטיסטיקה של ההקשר, לא על הבנה מלאה של אמת
- [ ] B. מסד נתונים שמחזיק עובדות אמתיות ומעדכן אותן בזמן אמת תמיד
- [ ] C. מערכת שמחליפה תמיד את שלבי בדיקות ה-production
- [ ] D. אלגוריתם שמחשב רק מספרים ואסור לכתוב בו טקסט

<small>נושא: ai_development::AI</small>

### 2. מתי נכון להשתמש ב-Cursor Cmd+K?

- [ ] A. כשיודעים איזה שינוי קטן רוצים לערוך במקום בקובץ הנוכחי
- [ ] B. כשצריך למחוק את כל ה-repo
- [ ] C. כשצריך להחליף את Git
- [ ] D. כשצריך לפרוס production בלי בדיקות

<small>נושא: ai_development::Cursor</small>

### 3. מה חסר ב-prompt חלש כמו 'תתקן את זה'?

- [ ] A. מטרה, context, מגבלות וקריטריון קבלה שאפשר לבדוק
- [ ] B. ניסוח רגשי יותר בלי לציין קובץ, מגבלה או בדיקה
- [ ] C. בחירת צבע Tailwind בלי קשר לבאג או לדרישת המשימה
- [ ] D. מזהה מספרי שרירותי שאינו מוסיף הקשר או קריטריון קבלה

<small>נושא: ai_development::Prompt Engineering</small>

### 4. מה AI Code Review צריך לבדוק מעבר ל-'הקוד נראה יפה'?

- [ ] A. לוגיקה, אבטחה, contract, בדיקות, edge cases ושינוי לא קשור
- [ ] B. רק מספר השורות בקובץ
- [ ] C. רק צבעי Tailwind
- [ ] D. רק אם יש emoji בתגובה

<small>נושא: ai_development::AI Code Review</small>

### 5. מה מאפיין את Windsurf Cascade?

- [ ] A. סוכן IDE שמבצע plan, קורא קבצים, מציע diff, מריץ כלים ומעדכן לפי התוצאות
- [ ] B. ספריית CSS ל-grid בלבד
- [ ] C. מסד נתונים וקטורי
- [ ] D. פקודת git אחת שמחליפה review

<small>נושא: ai_development::Windsurf</small>

### 6. מה Claude Code?

- [ ] A. CLI agent של Anthropic — autonomous software engineering
- [ ] B. Browser
- [ ] C. SaaS
- [ ] D. WordPress

<small>נושא: ai_development::Claude Code</small>

### 7. מה GitHub Copilot?

- [ ] A. AI code completion ב-IDE
- [ ] B. editor
- [ ] C. tab
- [ ] D. git plugin

<small>נושא: ai_development::Copilot</small>

### 8. מה context window ב-LLM?

- [ ] A. מספר tokens שהמודל יכול לעבד באותה עת (input + output)
- [ ] B. speed
- [ ] C. RAM
- [ ] D. cache

<small>נושא: ai_development::Context Window</small>

### 9. מה ChatGPT?

- [ ] A. chatbot של OpenAI מבוסס GPT models
- [ ] B. DB
- [ ] C. CLI
- [ ] D. plugin

<small>נושא: ai_development::ChatGPT</small>

### 10. מה hallucinations ב-AI?

- [ ] A. יציאות מומצאות: פונקציות/APIs שלא קיימים
- [ ] B. random
- [ ] C. slow
- [ ] D. errors

<small>נושא: ai_development::Hallucinations</small>

### 11. מהן מגבלות עיקריות של AI coding?

- [ ] A. hallucinations, knowledge cutoff, no global view, can't run code
- [ ] B. slow
- [ ] C. cost
- [ ] D. none

<small>נושא: ai_development::AI Limitations</small>

### 12. מה pattern של AI pair programming טוב?

- [ ] A. אתה מוביל, AI מציע. בודק טריוויאלית — מאשר מהיר. לוגיקה — בודק לעומק
- [ ] B. AI אוטונומי
- [ ] C. אני יחידי
- [ ] D. random

<small>נושא: ai_development::AI Pair Programming</small>

### 13. מה יודפס?

const arr = [1, 2, 3];
arr.push(4);
console.log(arr);

- [ ] A. [1, 2, 3]
- [ ] B. [1, 2, 3, 4]
- [ ] C. [4, 1, 2, 3]
- [ ] D. 4

<small>נושא: lesson_11::Array</small>

### 14. מה מחזירה filter?

- [ ] A. איבר אחד בלבד
- [ ] B. מערך חדש עם איברים שעברו תנאי
- [ ] C. מספר האיברים שנמחקו
- [ ] D. מחזירה רק מספרים

<small>נושא: lesson_11::filter</small>

### 15. מה ההבדל המרכזי בין map ל-forEach?

- [ ] A. map מוחק איברים; forEach מוסיף
- [ ] B. map יוצר מערך חדש; forEach רק רץ
- [ ] C. forEach מחזיר מערך חדש
- [ ] D. map עובד רק עם מספרים

<small>נושא: lesson_11::map</small>

### 16. מה התוצאה?

const sum = [1, 2, 3, 4].reduce((acc, n) => acc + n, 0);

- [ ] A. 0
- [ ] B. 10
- [ ] C. [1, 2, 3, 4]
- [ ] D. 4

<small>נושא: lesson_11::reduce</small>

### 17. מה יודפס?

const a = [1, 2];
const b = [...a, 3];
console.log(b);

- [ ] A. [1, 2]
- [ ] B. [1, 2, 3]
- [ ] C. a נשאר מערך פנימי
- [ ] D. שגיאה

<small>נושא: lesson_11::spread</small>

### 18. מה יודפס ראשון?

console.log('A');
setTimeout(() => console.log('B'), 1000);
console.log('C');

- [ ] A. A→B→C
- [ ] B. A→C→B
- [ ] C. B→A→C
- [ ] D. C→A→B

<small>נושא: lesson_15::setTimeout</small>

### 19. למה משתמשים ב-await?

- [ ] A. כדי לחכות לתוצאה של פעולה אסינכרונית
- [ ] B. כדי ליצור מערך
- [ ] C. כדי למחוק state
- [ ] D. כדי לעצב כפתור

<small>נושא: lesson_15::Asynchronous</small>

### 20. איזו מהבאות נכונה לטיפול בשגיאת fetch ב-async/await?

- [ ] A. if (fetch.error) ...
- [ ] B. try { await fetch(url) } catch (err) { ... }
- [ ] C. fetch(url).onerror = ...
- [ ] D. אי אפשר לתפוס שגיאות מ-fetch

<small>נושא: lesson_15::fetch</small>

### 21. מה יודפס?

const a = { v: 1 };
const b = a;
b.v = 99;
console.log(a.v);

- [ ] A. 1
- [ ] B. 99
- [ ] C. undefined
- [ ] D. { v: 1 }

<small>נושא: lesson_11::By Reference</small>

### 22. מה יודפס?

let a = 5;
let b = a;
a = 10;
console.log(b);

- [ ] A. 5
- [ ] B. 10
- [ ] C. undefined
- [ ] D. NaN

<small>נושא: lesson_11::By Value</small>

### 23. מהי המטרה של constructor במחלקה?

- [ ] A. להגדיר את השם של המחלקה
- [ ] B. פונקציה מיוחדת שרצה כשיוצרים מופע חדש (new)
- [ ] C. להגדיר את הירושה
- [ ] D. פונקציה סטטית

<small>נושא: lesson_13::class</small>

### 24. מתי רץ ה-cleanup function שמוחזר מ-useEffect?

- [ ] A. אחרי כל console.log
- [ ] B. לפני effect הבא או unmount
- [ ] C. מיד אחרי הרינדור
- [ ] D. רק ב-class components

<small>נושא: lesson_24::cleanup</small>

### 25. מה Closure?

- [ ] A. פונקציה שזוכרת את הסקופ שבו נוצרה
- [ ] B. פונקציה בלי פרמטרים
- [ ] C. פונקציה שלא מחזירה ערך
- [ ] D. מתודה של Object

<small>נושא: lesson_15::Closure</small>

### 26. איזו בעיה Context API פותר?

- [ ] A. העברת props דרך הרבה קומפוננטות ביניים
- [ ] B. שגיאות ב-CSS
- [ ] C. מחיקת קבצים ב-Node
- [ ] D. שמירה ב-MongoDB

<small>נושא: lesson_23::Context API</small>

### 27. מה ההבדל בין useEffect(fn) ל-useEffect(fn, [])?

- [ ] A. שניהם רצים רק ב-mount
- [ ] B. בלי deps רץ אחרי כל render; [] פעם אחת
- [ ] C. [] מריץ לפני render
- [ ] D. ללא מערך — לא רץ בכלל

<small>נושא: lesson_24::dependency array</small>

### 28. מה עושה הקוד?

document.getElementById('title').innerHTML = 'Hello';

- [ ] A. יוצר אלמנט חדש
- [ ] B. מוחק אלמנט
- [ ] C. משנה את התוכן של אלמנט עם id בשם title
- [ ] D. שומר מידע ב-localStorage

<small>נושא: lesson_13::getElementById</small>

### 29. מה הסדר הנכון ליצירת ושיוך אלמנט חדש?

- [ ] A. appendChild → createElement → innerHTML
- [ ] B. createElement → innerHTML → appendChild
- [ ] C. innerHTML → appendChild → createElement
- [ ] D. createElement → appendChild → removeChild

<small>נושא: lesson_13::createElement</small>

### 30. איך מגדירים נתיב דינמי לפרופיל משתמש?

- [ ] A. <Route path="/user" />
- [ ] B. <Route path="/user/:userId" />
- [ ] C. נתיב קבוע: <Route path="/user/userId" />
- [ ] D. קריאה ל-useParams('/user') במקום הגדרת Route

<small>נושא: lesson_23::dynamic route</small>

### 31. מתי נשתמש ב-useEffect?

- [ ] A. כשצריך לבצע פעולה בזמן טעינה או בעקבות שינוי
- [ ] B. כשצריך להגדיר enum
- [ ] C. כשצריך ליצור Collection
- [ ] D. רק לכתיבת CSS

<small>נושא: lesson_24::useEffect</small>

### 32. למה לא לעשות fetch ישירות בגוף הקומפוננטה?

- [ ] A. כי זה רץ בכל רינדור
- [ ] B. כי זה גורם ללולאת רינדורים אינסופית
- [ ] C. כי fetch לא עובד בריאקט
- [ ] D. כי אסור להביא מידע מ-API

<small>נושא: lesson_24::fetching data</small>

### 33. מה עושה useRef?

- [ ] A. שומר ref בלי לגרום לרינדור
- [ ] B. יוצר Route
- [ ] C. מוחק state
- [ ] D. מחליף MongoDB

<small>נושא: lesson_24::useRef</small>

### 34. מתי כדאי להשתמש ב-useMemo?

- [ ] A. כשצריך state חדש
- [ ] B. כשיש חישוב כבד שחוזר ברינדור
- [ ] C. כדי ליצור state
- [ ] D. רק עם setTimeout

<small>נושא: lesson_24::useMemo</small>

### 35. מה התפקיד של try/catch?

- [ ] A. ליצור קומפוננטה
- [ ] B. לטפל בשגיאות בלי לקרוס
- [ ] C. להריץ לולאה
- [ ] D. לשמור מידע בדפדפן

<small>נושא: lesson_15::try</small>

### 36. איזה אירוע מתאים ללחיצה על כפתור?

- [ ] A. click
- [ ] B. input
- [ ] C. load
- [ ] D. submit

<small>נושא: lesson_19::event</small>

### 37. ב-Express, איזה method מתאים ליצירת משאב חדש?

- [ ] A. GET
- [ ] B. POST
- [ ] C. DELETE
- [ ] D. PATCH

<small>נושא: lesson_17::POST</small>

### 38. מה תפקיד express.json()?

- [ ] A. מעצב את הדף
- [ ] B. Middleware שמפענח גוף JSON של בקשות
- [ ] C. מתחבר ל-MongoDB
- [ ] D. יוצר Route חדש

<small>נושא: lesson_17::body-parser</small>

### 39. מה משמעות סטטוס 404?

- [ ] A. הצלחה
- [ ] B. המשאב לא נמצא
- [ ] C. שגיאת שרת
- [ ] D. אין הרשאה

<small>נושא: lesson_17::Status Codes</small>

### 40. מה תפקיד extends?

- [ ] A. ירושה — מחלקה אחת מורישה תכונות לאחרת
- [ ] B. מאריכה את הזיכרון של המחלקה
- [ ] C. מוסיפה מתודות חדשות
- [ ] D. אין כזה דבר ב-JS

<small>נושא: lesson_13::extends</small>

---

## חלק ב' — שאלות השלמה (Fill)

### 1. 

```
const chain = prompt.pipe(model).pipe(____);
```

*רמז: השלב שממיר פלט מודל למבנה שהקוד יכול להשתמש בו.*

**תשובה:** ____________________

<small>נושא: lesson_ai_engineering::LangChain</small>

### 2. 

```
const modelName = process.env.____;
const result = await runModel(modelName, task);
```

*רמז: שם משתנה סביבה ששומר בחירת מודל.*

**תשובה:** ____________________

<small>נושא: lesson_ai_engineering::model selection</small>

### 3. 

```
const messages = [
  { role: "system", content: "Answer briefly" },
  { role: "____", content: question }
];
```

*רמז: role של הודעת המשתמש שנשלחת למודל.*

**תשובה:** ____________________

<small>נושא: lesson_ai_engineering::prompt messages</small>

### 4. 

```
const strategy = stableStyleNeeded && manyReviewedExamples ? "____" : "prompt-or-rag";
```

*רמז: בוחרים בזה רק כשיש הרבה דוגמאות בדוקות ודפוס קבוע.*

**תשובה:** ____________________

<small>נושא: lesson_ai_engineering::fine-tuning boundary</small>

### 5. 

```
const reply = await client.chat.completions.____({
  model: 'gpt-4o-mini',
  messages: [
    { role: 'system', content: 'You are a concise senior dev.' },
    { role: 'user', content: 'הסבר במשפט אחד מה זה closure.' }
  ],
  temperature: 0.3,
});
```

*רמז: השיטה נקראת completions בתוך קישור chat.*

**תשובה:** ____________________

<small>נושא: ai_development::AI</small>

### 6. 

```
# .cursorrules
- Use Tailwind.
- Do not add dependencies without ____.
```

*רמז: אישור מפורש לפני שינוי תלות.*

**תשובה:** ____________________

<small>נושא: ai_development::Cursor</small>

### 7. 

```
const prompt = { goal: 'fix bug', context: 'login form', constraints: '____' };
```

*רמז: מגבלה שמונעת הוספת תלויות.*

**תשובה:** ____________________

<small>נושא: ai_development::Prompt Engineering</small>

### 8. 

```
const review = ['logic', 'security', 'performance', '____'];
```

*רמז: בדיקות חסרות או שבורות הן חלק מ-review.*

**תשובה:** ____________________

<small>נושא: ai_development::AI Code Review</small>

### 9. 

```
const cascade = ['PLAN', 'READ', 'PROPOSE', 'APPLY', '____'];
```

*רמז: אחרי שינוי מריצים בדיקות או פקודה.*

**תשובה:** ____________________

<small>נושא: ai_development::Windsurf</small>

### 10. 

```
const nums = [1, 2, 3];
const doubled = nums.____(n => n * 2);
console.log(doubled); // [2, 4, 6]
```

*רמז: מתודה שיוצרת מערך חדש מטרנספורמציה.*

**תשובה:** ____________________

<small>נושא: lesson_11::map</small>

### 11. 

```
const nums = [1, 2, 3, 4];
const even = nums.____(n => n % 2 === 0);
// [2, 4]
```

*רמז: מתודה שמסננת איברים לפי תנאי boolean.*

**תשובה:** ____________________

<small>נושא: lesson_11::filter</small>

### 12. 

```
const sum = [1, 2, 3, 4].____((acc, n) => acc + n, 0); // 10
```

*רמז: מצמצמת מערך לערך אחד.*

**תשובה:** ____________________

<small>נושא: lesson_11::reduce</small>

### 13. 

```
const users = [{id:1, name:'A'}, {id:2, name:'B'}];
const u = users.____(u => u.id === 2);
console.log(u.name); // 'B'
```

*רמז: מחזירה את האיבר הראשון שעומד בתנאי.*

**תשובה:** ____________________

<small>נושא: lesson_11::find</small>

### 14. 

```
const arr = [1, 2];
arr.____(3);
console.log(arr); // [1, 2, 3]
```

*רמז: מוסיפה איבר לסוף המערך.*

**תשובה:** ____________________

<small>נושא: lesson_11::push</small>

### 15. 

```
const arr = [1, 2, 3];
const last = arr.____();
console.log(last); // 3
console.log(arr); // [1, 2]
```

*רמז: מסירה ומחזירה את האיבר האחרון.*

**תשובה:** ____________________

<small>נושא: lesson_11::pop</small>

### 16. 

```
const arr = [1, 2, 3];
const first = arr.____();
console.log(first); // 1
```

*רמז: מסירה ומחזירה את האיבר הראשון.*

**תשובה:** ____________________

<small>נושא: lesson_11::shift</small>

### 17. 

```
const nums = [3, 1, 4, 1, 5];
nums.____((a, b) => a - b);
console.log(nums); // [1, 1, 3, 4, 5]
```

*רמז: ממיינת את המערך במקום עם פונקציית השוואה.*

**תשובה:** ____________________

<small>נושא: lesson_11::sort</small>

### 18. 

```
async function getUsers() {
  const res = ____ fetch('/api/users');
  return res.json();
}
```

*רמז: מילת מפתח שמחכה ל-Promise בתוך async function.*

**תשובה:** ____________________

<small>נושא: lesson_15::Asynchronous</small>

### 19. 

```
fetch('/api')
  .then(res => res.____())
  .then(data => console.log(data));
```

*רמז: מתודה שמפענחת את גוף התגובה כ-JSON.*

**תשובה:** ____________________

<small>נושא: lesson_15::fetch</small>

### 20. 

```
const p = new Promise((____, reject) => {
  setTimeout(() => /* fulfill with 'hello' */, 1000);
});
```

*רמז: שם הפרמטר הראשון בקולבק של new Promise (קונבנציה).*

**תשובה:** ____________________

<small>נושא: lesson_15::Promise</small>

---

## מפתח תשובות

**MC:**

1. A — מודל שמחזיר טקסט/קוד בהסתמך על סטטיסטיקה של ההקשר, לא על הבנה מלאה של אמת
   *הסבר:* השיעור מדגיש ש-LLM מנחש רצף tokens בהתאם להקשר ואחוזי הסתברות, ולכן צריך תמיד לאמת את הפלט.
2. A — כשיודעים איזה שינוי קטן רוצים לערוך במקום בקובץ הנוכחי
   *הסבר:* Cmd+K מתאים לעריכה מקומית וממוקדת. למשימות רב-קבציות משתמשים בצ'אט/Composer וקוראים diff.
3. A — מטרה, context, מגבלות וקריטריון קבלה שאפשר לבדוק
   *הסבר:* Prompt טוב מצמצם ניחוש: מה המטרה, באיזה קוד, אילו מגבלות ומה נחשב פתרון שעבר.
4. A — לוגיקה, אבטחה, contract, בדיקות, edge cases ושינוי לא קשור
   *הסבר:* Review איכותי מחפש סיכון התנהגותי, לא רק סגנון. AI יכול לסייע, אבל האדם מאשר.
5. A — סוכן IDE שמבצע plan, קורא קבצים, מציע diff, מריץ כלים ומעדכן לפי התוצאות
   *הסבר:* Cascade עובד בלולאת agent: תכנון, tool calls, תצפית ותכנון מחדש עם אישור אנושי בגבולות כתיבה.
6. A — CLI agent של Anthropic — autonomous software engineering
   *הסבר:* Claude עם tool use: bash, read/write files, agent loops.
7. A — AI code completion ב-IDE
   *הסבר:* VS Code, JetBrains. completions + chat.
8. A — מספר tokens שהמודל יכול לעבד באותה עת (input + output)
   *הסבר:* GPT-4: 128K, Claude Opus: 200K-1M. גדול יותר → context עשיר אבל יקר/איטי.
9. A — chatbot של OpenAI מבוסס GPT models
   *הסבר:* GPT-4o, GPT-5. גם API.
10. A — יציאות מומצאות: פונקציות/APIs שלא קיימים
   *הסבר:* בקוד: שמות פונקציות שלא קיימים. פתרון: docs + tests.
11. A — hallucinations, knowledge cutoff, no global view, can't run code
   *הסבר:* AI לא מבצע בעצמו ב-default.
12. A — אתה מוביל, AI מציע. בודק טריוויאלית — מאשר מהיר. לוגיקה — בודק לעומק
   *הסבר:* trust + verify. AI מצוין בroutine.
13. B — [1, 2, 3, 4]
   *הסבר:* push מוסיפה לסוף המערך ומשנה את המקור.
14. B — מערך חדש עם איברים שעברו תנאי
   *הסבר:* filter מחזירה מערך חדש המכיל רק את האיברים שלגביהם הקולבק החזיר true. לא משנה את המקור.
15. B — map יוצר מערך חדש; forEach רק רץ
   *הסבר:* map בונה מערך חדש מהתוצאות של הקולבק. forEach רק מבצע פעולה לכל איבר ומחזיר undefined.
16. B — 10
   *הסבר:* reduce מצמצם את המערך לערך אחד — סכום כל האיברים החל מ-0.
17. B — [1, 2, 3]
   *הסבר:* spread (...) פורש את a לתוך b ויוצר מערך חדש [1, 2, 3].
18. B — A→C→B
   *הסבר:* setTimeout נדחה ל-event loop. הקוד הסינכרוני (A,C) רץ קודם, ואז B.
19. A — כדי לחכות לתוצאה של פעולה אסינכרונית
   *הסבר:* await ממתין ל-Promise להיפתר ומחזיר את הערך — אפשר רק בתוך async function.
20. B — try { await fetch(url) } catch (err) { ... }
   *הסבר:* ב-async/await תמיד עוטפים ב-try/catch כדי לתפוס דחיות של ה-Promise.
21. B — 99
   *הסבר:* אובייקטים מועתקים By Reference. b מצביע על אותו אובייקט כמו a.
22. A — 5
   *הסבר:* ערכים פרימיטיביים מועתקים By Value. שינוי a לא משפיע על b.
23. B — פונקציה מיוחדת שרצה כשיוצרים מופע חדש (new)
   *הסבר:* constructor רץ אוטומטית כשעושים `new MyClass(...)` ומאתחל את המופע.
24. B — לפני effect הבא או unmount
   *הסבר:* Cleanup חיוני להסרת event listeners, ביטול timers, סגירת WebSockets — נמנע מזליגות זיכרון.
25. A — פונקציה שזוכרת את הסקופ שבו נוצרה
   *הסבר:* Closure 'זוכר' את הסביבה הלקסיקלית — חיוני ל-private state, partial application, modules.
26. A — העברת props דרך הרבה קומפוננטות ביניים
   *הסבר:* Context מספק ערך גלובלי לעץ קומפוננטות, חוסך 'prop drilling'.
27. B — בלי deps רץ אחרי כל render; [] פעם אחת
   *הסבר:* [] = run once on mount. [a, b] = run when a or b change. (none) = run after every render.
28. C — משנה את התוכן של אלמנט עם id בשם title
   *הסבר:* getElementById מאתר את האלמנט; innerHTML מציב לו תוכן חדש.
29. B — createElement → innerHTML → appendChild
   *הסבר:* קודם יוצרים, אז קובעים תוכן, ולבסוף מצרפים ל-DOM.
30. B — <Route path="/user/:userId" />
   *הסבר:* :userId הוא פרמטר דינמי. בקומפוננטה: const { userId } = useParams().
31. A — כשצריך לבצע פעולה בזמן טעינה או בעקבות שינוי
   *הסבר:* useEffect מיועד לתופעות לוואי: fetch, טיימרים, האזנות. רץ אחרי הרינדור.
32. B — כי זה גורם ללולאת רינדורים אינסופית
   *הסבר:* fetch בגוף הקומפוננטה רץ בכל רינדור → setState → רינדור → fetch... לולאה. useEffect עם dependency ריק רץ פעם אחת.
33. A — שומר ref בלי לגרום לרינדור
   *הסבר:* useRef מחזיר אובייקט { current } שאפשר לשנות בלי לגרום לרינדור — מצוין לאלמנטי DOM ולערכים מתמשכים.
34. B — כשיש חישוב כבד שחוזר ברינדור
   *הסבר:* useMemo מזכיר תוצאת חישוב יקר; מיותר לערכים פשוטים. תלוי ב-dependency array.
35. B — לטפל בשגיאות בלי לקרוס
   *הסבר:* try מריץ קוד שיכול להיכשל; catch תופס את השגיאה ומאפשר טיפול ידידותי.
36. A — click
   *הסבר:* click הוא האירוע הסטנדרטי ללחיצת עכבר על כל אלמנט.
37. B — POST
   *הסבר:* POST משמש ליצירה. GET לקריאה, PUT/PATCH לעדכון, DELETE למחיקה.
38. B — Middleware שמפענח גוף JSON של בקשות
   *הסבר:* express.json() מפענח את req.body כשהלקוח שולח Content-Type: application/json.
39. B — המשאב לא נמצא
   *הסבר:* 404 Not Found — הבקשה תקינה אך המשאב המבוקש לא קיים.
40. A — ירושה — מחלקה אחת מורישה תכונות לאחרת
   *הסבר:* class Dog extends Animal — Dog מקבלת את כל המתודות של Animal.

**Fill:**

1. `parser`
   *הסבר:* LangChain מחבר שלבים: prompt, model ו-parser. הערך שלו מופיע כשיש רצף פעולות ולא קריאה אחת פשוטה.
2. `OPENAI_MODEL`
   *הסבר:* model selection צריך להיות configurable כדי להחליף איכות, latency ועלות בלי לשכתב קוד.
3. `user`
   *הסבר:* prompt messages מסדרים למודל תפקידים: system קובע כללים, user מביא את השאלה, assistant מייצג תשובות קודמות.
4. `fine-tuning`
   *הסבר:* fine-tuning מתאים לשינוי התנהגות עקבי של מודל בעזרת דוגמאות רבות. ידע חיצוני ועדכני עדיף לפתור עם RAG.
5. `create`
   *הסבר:* client.chat.completions.create הוא ה-API המלא לבקשת completion מהשיחה.
6. `approval`
   *הסבר:* .cursorrules מתאים לכללי פרויקט קבועים. כלל כזה מונע מ-Cursor להוסיף dependency בלי אישור.
7. `noNewDeps`
   *הסבר:* Prompt טוב כולל constraints. noNewDeps מבהיר שהפתרון לא אמור להוסיף package.
8. `tests`
   *הסבר:* AI Code Review טוב לא מסתפק בסגנון; הוא מחפש גם כיסוי בדיקות ורגרסיות.
9. `RUN`
   *הסבר:* Windsurf Cascade יכול לתכנן, לערוך ואז להריץ פקודות כדי לצפות בתוצאה.
10. `map`
   *הסבר:* map מחזיר מערך חדש עם תוצאת הפונקציה לכל איבר.
11. `filter`
   *הסבר:* filter מחזיר מערך חדש עם איברים שהקולבק החזיר עליהם true.
12. `reduce`
   *הסבר:* reduce מקבלת accumulator וערך התחלתי, ובונה תוצאה צוברת.
13. `find`
   *הסבר:* find מחזיר את האיבר הראשון או undefined.
14. `push`
   *הסבר:* push משנה את המערך במקום ומחזירה את האורך החדש.
15. `pop`
   *הסבר:* pop משנה את המערך במקום ומחזירה את האיבר שהוסר.
16. `shift`
   *הסבר:* shift מסיר את [0]. unshift מוסיף ל-[0].
17. `sort`
   *הסבר:* ללא comparator — sort ממיינת לפי מחרוזות. עם (a,b)=>a-b — מיון מספרי עולה.
18. `await`
   *הסבר:* await ממתין לפענוח של Promise ומחזיר את הערך.
19. `json`
   *הסבר:* res.json() מחזיר Promise שמתפענח לאובייקט/מערך JS.
20. `resolve`
   *הסבר:* הקולבק של new Promise מקבל שני פרמטרים — הראשון (resolve) להצלחה, השני (reject) לכישלון. קוראים ל-resolve(value) כדי להפוך את ה-Promise ל-fulfilled.

**עודכן אוטומטית:** 2026-05-02T13:11:04.576Z