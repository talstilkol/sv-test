4. **מלכודת נפוצה**  
שכיח: לשכוח את `module.exports = router;` – בלי זה ה-router לא ייכנס לשימוש בקובץ הראשי.  
שכיח: לא להחזיר JSON – חובה להשתמש ב-`res.json()` או `res.send()` עם אובייקט/מערך.  
שכיח: לשכוח את `status(200)` – хотя Express מוסיף 200 כברירת מחדל, מומלץ להדגיש את הסטטוס במפורש לברירות מחדל ברורות.

5. **בדיקת תקינות**  
- שליחת בקשת `GET /` תחזיר את המערך `items` בפורמט JSON.
- נוכל לבדוק עם `curl`: